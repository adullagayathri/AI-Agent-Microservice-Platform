version: "3"

services:

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  orchestrator:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis

  agent:
    build: .
    command: python agents/agent_worker.py
    depends_on:
      - redis